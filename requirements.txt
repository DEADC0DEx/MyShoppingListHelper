python-telegram-bot==20.7
requests==2.31.0
flask>=3.0.0

# OCR for recipe images (optional but recommended)
# Also requires system packages:
#   sudo apt install tesseract-ocr tesseract-ocr-heb
pytesseract>=0.3.10
Pillow>=10.0.0
pytesseract==0.3.13
Pillow==10.3.0

# PDF parsing for receipt import
# Install: pip install PyMuPDF
PyMuPDF>=1.23.0
